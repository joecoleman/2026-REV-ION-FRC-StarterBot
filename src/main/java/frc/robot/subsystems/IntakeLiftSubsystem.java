package frc.robot.subsystems;

import edu.wpi.first.wpilibj2.command.SubsystemBase;

/**
 * IntakeLiftSubsystem has been removed. This stub prevents compilation errors in case
 * some files still reference the type. Do not instantiate — the real subsystem was deleted.
 */
@Deprecated
public final class IntakeLiftSubsystem extends SubsystemBase {
    public IntakeLiftSubsystem() {
        throw new UnsupportedOperationException("IntakeLiftSubsystem was removed; do not instantiate.");
    }
}
